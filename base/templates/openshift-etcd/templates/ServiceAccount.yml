{{- if .Values.etcdbackup.include }}

---
kind: ServiceAccount
apiVersion: v1
metadata:
  name: {{ .Values.etcdbackup.serviceAccount }}
  namespace: {{ .Values.etcdbackup.namespace }}
  labels:
    {{- include ".Values.etcdbackup.labels" . | nindent 4 }}

{{ end }}
